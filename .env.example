# Copy to .env — playwright.config.ts loads it automatically (Node's process.loadEnvFile()).
# CI sets variables directly (repository variable BASE_URL in .github/workflows/tests.yml).

# Application base URL used by page.goto('/...') (default: https://playwright.dev)
BASE_URL=https://playwright.dev
